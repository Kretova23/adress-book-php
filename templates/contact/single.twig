{% extends "layout.twig" %}

{% block content %}

    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Номер Контакта {{ data.id }}</h4>
            <p class="card-text">ФИО: {{ data.SecondName }} {{ data.name }} {{  data.ThirdName }}</p>
        </div>
    </div>

{% endblock %}