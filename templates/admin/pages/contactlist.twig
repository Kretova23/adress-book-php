{% extends "/admin/main.twig" %}

{% block content %}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Список Контактов</h4>
            <p class="card-description"><a href="contact/add">Добавить новый контакт</a></code>
            </p>
            <table class="table table-dark">
                <thead>
                <tr>
                    <th> Id </th>
                    <th> Ф.И.О. </th>
                    <th>  </th>
                    <th>  </th>
                </tr>
                </thead>
                <tbody>
                {% for contact in contacts %}


                <tr>
                    <td> {{ contact.Id }} </td>
                    <td> {{ contact.fio }}  </td>
                    <td><a href="contact/edit/{{ contact.Id }} ">Edit</a></td>
                    <td> <a href="contact/delete/{{ contact.Id }} ">Delete</a></td>
                </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

{% endblock %}